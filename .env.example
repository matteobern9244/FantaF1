MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/fantaf1_dev?retryWrites=true&w=majority&appName=Cluster0
ADMIN_SESSION_SECRET=<long-random-secret>
VITE_APP_LOCAL_NAME=<optional-local-hero-title>

# Local development overrides
ASPNETCORE_ENVIRONMENT=Development
PORT=3001

# Database override for local development (fantaf1_dev) or CI (fantaf1_ci).
# Do not use for Render staging/production.
MONGODB_DB_NAME_OVERRIDE=fantaf1_dev
